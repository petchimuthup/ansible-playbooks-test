---
 - name: Modify sssd and sudoers conf file
   hosts: ansmgdnode01
   tasks:

    - name: backup sssd and sudoers conf file
      copy: 
        src: "{{ item.src }}" 
        dest: "{{ item.dest }}"
        remote_src: yes
      with_items:
        - { src: '/root/data/file',dest: '/etc/file_bkp' }


      
      
...

