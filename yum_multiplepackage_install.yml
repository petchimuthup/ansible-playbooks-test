---
 - name: multiple package install
   hosts: all
   tasks:

    - name: install vsftpd & telnet
      yum:
        name: "{{ packages }}"
      vars:
        packages:
        - vsftpd
        - telnet


...
